**Skeleton** — placeholder con shimmer para contenido de forma conocida.

```jsx
<Skeleton variant="block" height={90} />
<Skeleton variant="line" width="70%" />
<Skeleton variant="pill" />
```

Úsalo en listas, tablas y detalle para preservar el layout mientras carga. Para acción puntual sin layout, usa Spinner.
